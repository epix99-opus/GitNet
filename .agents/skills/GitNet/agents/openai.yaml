interface:
  display_name: "GitNet"
  short_description: "Repo-specific patterns and workflows for GitNet"
  default_prompt: "Use the GitNet repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true