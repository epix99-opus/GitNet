# 三机 Git 仓库盘点表（模板）

> **用法**：复制本文件为 `inventory-<hostname>.md`（如 `inventory-epix.md`），在 **该主机** 上按 [94-multi-node-agent-inventory-raci-and-config-matrix.md](../94-multi-node-agent-inventory-raci-and-config-matrix.md) §5 填表。**勿**将口令、PAT、私钥、带 token 的 URL 写入本仓库。

## 主机元数据

| 字段 | 填写 |
|------|------|
| **主机名（Tailscale）** | |
| **OS** | |
| **填写人 / 日期** | |

## 已安装的编程 Agent 工具（实机）

| 工具 | 已安装？（是/否） | 主要工作目录（若适用） |
|------|------------------|------------------------|
| Cursor | | |
| Codex CLI | | |
| Claude Code | | |

## 仓库清单（每个 Git 仓库一行）

| 仓库根路径（`git rev-parse --show-toplevel`） | 当前分支 | `origin` URL（可脱敏） | `origin` 是否 bare 目标（是/否/未知） | `user.name` 来源片段 | 命中 Agent？（cursor/codex/claude-code/人类） | 最后提交（一行） | 备注 |
|---------------------------------------------|----------|------------------------|----------------------------------------|----------------------|----------------------------------------------|------------------|------|
| | | | | | | | |
| | | | | | | | |
| | | | | | | | |

## 与规范差距（自评）

| 仓库路径 | L1 身份 | L2 对象 | 计划整改 |
|----------|---------|---------|----------|
| | 通过/否 | 通过/否 | |

## 修订记录

- （填写后在此记日期与摘要）
